en:
  chroma:
    header_regex: 'header_regex: regular expression to identify the header row'
    header_column_regex: 'header_column_regex: regular expression to identify the header column separator (overwrites column_regex)'
    header_skip_column: 'header_skip_column: array of indexes of columns to ignore (overwrites skip_column)'
    header_append: 'header_append: array of elements to append to the header'
    header_prepend: 'header_prepend: array of elements to prepend to the header'
    header_sort: 'header_sort: array of strings to specify the order of columns'
    row_regex: 'row_regex: regular expression to indentify data rows'
    column_regex: 'column_regex: regular expression to identify the column separator'
    skip_column: 'skip_column: array of indexes of columns to ignore'
    should_scrub_re: 'should_scrub_re: regular expression to remove any ancor from data rows'
    reject_sample_regex: 'reject_sample_regex: regular expression to reject sample-ids'
